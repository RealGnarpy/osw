const BUILD_PREFIX = "osw!";
const MAJOR_VERSION = 1;
const MINOR_VERSION = 12;
const PATCH_VERSION = .9;
const BUILD_METADATA = "release";
const Version = `${BUILD_PREFIX}${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}${BUILD_METADATA}`;
export {Version};